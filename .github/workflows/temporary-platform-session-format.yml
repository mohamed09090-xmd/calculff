name: Temporary platform session format patch

on:
  pull_request:

permissions:
  contents: read

jobs:
  format-patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.0'
          channel: stable
          cache: true

      - name: Generate Dart format patch
        shell: bash
        run: |
          set -euo pipefail
          dart format \
            lib/features/admin_platform/infrastructure/common/supabase_platform_error_mapper.dart \
            lib/features/admin_platform/application/common \
            test/features/admin_platform/infrastructure/supabase_platform_error_mapper_test.dart \
            test/features/admin_platform/application
          git diff --binary -- \
            lib/features/admin_platform/infrastructure/common/supabase_platform_error_mapper.dart \
            lib/features/admin_platform/application/common \
            test/features/admin_platform/infrastructure/supabase_platform_error_mapper_test.dart \
            test/features/admin_platform/application \
            > dart-format.patch
          test -s dart-format.patch

      - name: Upload Dart format patch
        uses: actions/upload-artifact@v4
        with:
          name: platform-session-dart-format-patch
          path: dart-format.patch
          if-no-files-found: error
          retention-days: 1
