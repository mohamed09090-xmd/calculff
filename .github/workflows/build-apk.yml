name: Verify and build signed Android release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow paused while direct credit sales and direct products are implemented."
